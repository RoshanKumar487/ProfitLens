# Brevo (or other SMTP provider) Credentials
# Found in your provider's dashboard under SMTP & API settings.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM_NAME="Your Company Name"
SMTP_FROM_EMAIL=
SMTP_SECURE=true # Use true for ports 465/587, false for others
